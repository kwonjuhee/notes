import type { Meta, StoryObj } from "@storybook/react";
import { TocHeader } from "./TocHeader";

const meta: Meta<typeof TocHeader> = {
  component: TocHeader,
};
export default meta;

export const Primary: StoryObj<typeof TocHeader> = {};
